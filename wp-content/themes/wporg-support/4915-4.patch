Index: comments.php
===================================================================
--- comments.php	(nonexistent)
+++ comments.php	(working copy)
@@ -0,0 +1,89 @@
+<?php
+/**
+ * The template file for displaying the comments and comment form
+ *
+ * @package WPBBP
+ */
+
+/*
+ * If the current post is protected by a password and
+ * the visitor has not yet entered the password we will
+ * return early without loading the comments.
+*/
+if ( post_password_required() ) {
+	return;
+}
+
+$post_id = get_the_ID();
+
+$feedback_description = __( 'Feedback you send to us will go only to the folks who maintain documentation. They may reach out in case there are questions or would like to followup feedback. But that too will stay behind the scenes.', 'wporg-forums' );
+
+if ( is_user_logged_in() ) :
+	if ( comments_open() || pings_open() ) :
+		$user          = wp_get_current_user();
+		$user_identity = $user->exists() ? $user->display_name : '';
+
+		comment_form(
+			array(
+				'title_reply'  => __( 'Was this article helpful? How could it be improved?', 'wporg-forums' ),
+				'comment_field' => sprintf(
+					'<p class="comment-form-comment">%s %s</p>',
+					sprintf(
+						'<label for="comment" class="screen-reader-text">%s</label>',
+						_x( 'Feedback', 'noun', 'wporg-forums' )
+					),
+					'<textarea id="comment" name="comment" cols="45" rows="8" maxlength="65525" required="required"></textarea>'
+				),
+				'logged_in_as'         => sprintf(
+					'<p>%1$s</p><p class="logged-in-as">%2$s</p>',
+					$feedback_description,
+					sprintf(
+						/* translators: 1: Edit user link, 2: Accessibility text, 3: User name, 4: Logout URL. */
+						__( '<a href="%1$s" aria-label="%2$s">Logged in as %3$s</a>. <a href="%4$s">Log out?</a>', 'wporg-forums' ),
+						get_edit_user_link(),
+						/* translators: %s: User name. */
+						esc_attr( sprintf( __( 'Logged in as %s. Edit your profile.', 'wporg-forums' ), $user_identity ) ),
+						$user_identity,
+						/** This filter is documented in wp-includes/link-template.php */
+						wp_logout_url( apply_filters( 'the_permalink', get_permalink( $post_id ), $post_id ) )
+					)
+				),
+				'label_submit' => __( 'Send Feedback', 'wporg-forums' )
+			)
+		);
+
+	elseif ( is_single() ) :
+	?>
+
+		<div class="comment-respond" id="respond">
+
+			<p class="comments-closed"><?php _e( 'Comments are closed.', 'wporg-forums' ); ?></p>
+
+		</div><!-- #respond -->
+
+	<?php
+	endif;
+
+else :
+?>
+
+	<div class="comment-respond" id="respond">
+
+		<h3 class="comment-reply-title"><?php _e( 'Was this article helpful? How could it be improved?', 'wporg-forums' ); ?></h3>
+		<?php
+		printf(
+			'<p>%1$s</p><p class="must-log-in">%2$s</p>',
+			$feedback_description,
+			sprintf(
+				/* translators: %s: Login URL. */
+				__( 'Please <a href="%s">log in</a> to leave the feedback.' ),
+				/** This filter is documented in wp-includes/link-template.php */
+				wp_login_url( apply_filters( 'the_permalink', get_permalink( $post_id ), $post_id ) )
+			)
+		);
+		?>
+
+	</div><!-- #respond -->
+
+<?php
+endif;
Index: functions.php
===================================================================
--- functions.php	(revision 10189)
+++ functions.php	(working copy)
@@ -888,3 +888,11 @@
  * Include the Strings for the supporg/update-php page.
  */
 include_once __DIR__ . '/helphub-update-php-strings.php';
+/*
+ * Support HTML5 for comments template.
+ */
+add_theme_support( 'html5',
+	array(
+		'comment-form'
+	)
+);
Index: sass/forms/_fields.scss
===================================================================
--- sass/forms/_fields.scss	(revision 10189)
+++ sass/forms/_fields.scss	(working copy)
@@ -184,6 +184,10 @@
 	overflow: auto;
 	padding: 2px 6px;
 	resize: vertical;
+
+	.comment-form-comment & {
+		width: 100%;
+	}
 }

 input[type="file"] {
Index: sass/site/_helphub.scss
===================================================================
--- sass/site/_helphub.scss	(revision 10189)
+++ sass/site/_helphub.scss	(working copy)
@@ -120,12 +120,16 @@
 					overflow: hidden;
 				}

-				 @media screen and ( min-width: $ms-breakpoint ) {
-					 margin: 0;
-					 width: 65%;
-				 }
-			 }
-		 }
-	 }
+				@media screen and ( min-width: $ms-breakpoint ) {
+					margin: 0;
+					width: 65%;
+				}
+			}
+		}
+	}
+
+	.comment-respond {
+		padding-bottom: 2rem;
+	}
 }

Index: sass/typography/_headings.scss
===================================================================
--- sass/typography/_headings.scss	(revision 10189)
+++ sass/typography/_headings.scss	(working copy)
@@ -31,6 +31,11 @@
 h3 {
 	font-size: ms( 4 );
 	font-weight: 400;
+
+	&.comment-reply-title {
+		font-size: ms( 2 );
+		font-weight: 700;
+	}
 }

 h4 {
Index: single.php
===================================================================
--- single.php	(revision 10189)
+++ single.php	(working copy)
@@ -17,6 +17,10 @@
 				the_post();

 				get_template_part( 'template-parts/content', 'single' );
+
+				if ( comments_open() || get_comments_number() ) :
+					comments_template();
+				endif;
 			endwhile; // End of the loop.
 			?>
 		</div>